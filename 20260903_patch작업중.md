# 2026-09-03 Patch 작업중

## 개발 기준
- 프로젝트: 아르메블랑쉐
- Repository: `g9inlife-blip/arme`
- 오프라인 전환 작업을 진행 중이며, 현재 핵심 대상은 `GameUpdateService`의 시작/리소스 업데이트 판정 흐름이다.
- 기존에 효과가 확인된 `noNeed` 관련 처리는 유지한다. 해당 부분은 다시 변경하지 않는다.
- 현재 바이너리 패치는 확실하게 적용되어 있는 상태를 기준으로 다음 분석을 진행한다.

## 현재 확인된 핵심 함수

### GameUpdateService
- `0x01780540` `StartCheckAssets`
- `0x017805D8` `CheckAssets`
- `0x017806D0` `isPackNeedUpdate`
- `0x01780754` `isAssetNeedUpdate_OnlyCheckVerion`
- `0x01780868` `isAssetNeedUpdate_CheckCacheAndBuild`
- `0x017808E8` 이후 버전/캐시/빌드 비교 로직
- `0x01781DF0` `GameUpdateService.d__75::MoveNext` — CheckAssets coroutine
- `0x01784218` `GameUpdateService.d__54::MoveNext` — VersionCheckUpdate coroutine
- `0x01784...` `GameUpdateService.<>d__66::MoveNext` — OnUpdateSuccessIE coroutine

## UpdateType 흐름
`CheckAssets` 성공 콜백에서 서버/로컬 판정 결과를 `GameUpdateService +0x78`에 저장한다.

- `UpdateType == 0`: 업데이트 없음 / 정상 완료 경로
- `UpdateType == 1`: 버전 저장 경로
- `UpdateType == 2`: 리소스/AssetBundle 업데이트 경로

`<>b__57_0`은 단순히 `+0x78`에 UpdateType을 저장하고 로그를 출력한다.

## 확인된 리소스 업데이트 진입점
`GameUpdateService.<>d__66::MoveNext`에서:

- `+0x78 == 2`이면
  - `set_UpdateStepDescription(..., StringLiteral_10881)`
  - `set_UpdateSteping(..., 7)`
  - 다음 상태로 yield
- 따라서 사용자에게 보이는 **"리소스파일 업데이트"**는 이 `UpdateType == 2` 경로와 직접 연결되어 있다.

## 세 가지 업데이트 판정 함수

### 1. isPackNeedUpdate — `0x017806D0`
서버 `Response_Allin1.IsUpgrade` (`+0x20`)를 반환한다.
Lua delegate가 있으면 delegate 결과를 사용한다.

### 2. isAssetNeedUpdate_OnlyCheckVerion — `0x01780754`
버전 비교 기반의 Asset 업데이트 필요 여부를 판단한다.

### 3. isAssetNeedUpdate_CheckCacheAndBuild — `0x01780868`
캐시/AssetBundle 정보/BuildVersion/저장된 AssetVersion 등을 비교한다.
확인된 조건:
- CacheManifest `UpdateAvailable`
- CacheAssetBundleInfo `UpdateAvailable`
- `BuildVersion > BuildVersionBindings`
- `remoteAssetVersion != AssetVersionSaved`
중 하나라도 true면 업데이트 필요로 반환한다.
또한 서비스 TypeInfo `+0x160`에 Lua delegate가 있으면 해당 delegate 결과가 우선된다.

## 현재까지 적용/검토한 패치

### VersionCheckUpdate HTTP error branch
`0x01784378`의 `isError` 검사 자체는 유지하고, error branch 진입 시 `x19`을 덮어쓰는 명령을 제거하는 방식으로 다음 패치를 검토/적용했다.

원본:
```asm
0178437c 73 12 40 f9    ldr x19,[x19,#0x20]
```

패치:
```asm
0178437c c3 00 00 14    b 0x01784688
```

목적:
- HTTP error가 발생했을 때 실패 콜백으로 바로 내려가지 않고 success callback(UpdateType 0)을 호출하는 경로를 시도.
- 단, 이후 실제 실행 결과에서 여전히 "리소스파일 업데이트" → progress 0 → "업데이트 실패"가 발생했으므로 이 패치만으로는 최종 해결되지 않았다.

### 중요
현재 사용자가 **기존 패치가 확실히 적용되어 있다**고 확인했으므로, 다음 분석에서는 "패치가 적용되지 않았다"는 가정을 하지 않는다.

## VersionCheckUpdate 핵심 흐름
`0x01784218` `d__54::MoveNext`:

1. `AppConst::get_Mark()`
2. `ProtocolGame_HttpRequest::V3_POST_AllInOne(mark,0)`
3. `IsDone` 대기
4. `isError` 검사
5. 정상 응답이면 `downloadHandler.text`
6. `Response_Allin1::GetByJson(text)`
7. `GamePlayerInfomation::set_ServerVersionManifest(...)`
8. 이후 CheckAssets 성공 callback으로 UpdateType 전달

HTTP error 관련 Ghidra listing:
```asm
01784338 f8 03 13 aa     mov x24,x19
0178433c 15 8f 43 f8     ldr x21,[x24,#0x38]!
01784340 08 00 80 12     mov w8,#0xffffffff
01784344 08 83 1d b8     stur w8,[x24,#-0x28]
01784348 75 00 00 b5     cbnz x21,LAB_01784354
01784354 e0 03 15 aa     mov x0,x21
01784358 b2 01 00 94     bl AliothEngine.Net.HttpRequest$$get_IsDone
0178435c a0 03 00 36     tbz w0,#0,LAB_017843d0
01784370 e0 03 15 aa     mov x0,x21
01784374 c7 01 00 94     bl AliothEngine.Net.HttpRequest$$get_isError
01784378 40 03 00 36     tbz w0,#0,LAB_017843e0
```

## OnUpdate 관련 함수
- `OnAssetUpdate`: `+0x58` completion callback invoke
- `OnPackUpdate`: `+0x50` completion callback invoke
- `OnUpdateFail`: `+0x60` failure callback invoke; UpdateStepDescription에 오류 문자열 설정
- `OnUpdateSuccess`: 시작 `OnUpdateSuccessIE` coroutine

## Response_Allin1 구조
```csharp
public class Response_Allin1 : IDisposable {
    public int Status; // 0x10
    public NoticeState NoticeStatus; // 0x14
    public string NoticeTime; // 0x18
    public bool IsUpgrade; // 0x20
    public List<Response_Allin1.AssetCDN> Assets; // 0x28
    public List<Response_Allin1.KeyUrl> Keys; // 0x30
    public List<Response_Allin1.Server> Servers; // 0x38
    public Dictionary<string,string> Http_API_URL; // 0x40
    public Version AssetVersion; // 0x48
    public Response_Allin1.AssetCDN BestCDN; // 0x50
}
```

`GetByJson`은 기본적으로 `JsonUtility::FromJson<Response_Allin1>`을 사용한다. `OnAfterDeserialize`는 주로 `Http_API_URL` dictionary 구성 및 hotfix delegate 처리이며, 현재 직접 패치 대상이 아니다.

## 현재 증상
- 기존 패치가 확실히 적용된 상태.
- 실행 시 여전히 **"리소스파일 업데이트"** 표시.
- progress가 **0**에 머무름.
- 이후 **"업데이트 실패, 다시 시도하겠습니까?"** 발생.

## 다음 분석 방향
1. `UpdateType == 2`가 실제로 어디서 결정되는지 `CheckAssets` 전체 호출/판정 순서를 다시 확인한다.
2. 세 번째 판정 함수인 `0x01780868`의 실제 호출 지점과 반환값 소비 지점을 추적한다.
3. `isPackNeedUpdate` / `isAssetNeedUpdate_OnlyCheckVerion` / `isAssetNeedUpdate_CheckCacheAndBuild`의 반환값이 어떤 순서와 조건으로 `UpdateType`으로 합쳐지는지 확인한다.
4. 특히 `0x01780868`의 결과가 `UpdateType == 2`로 연결되는 정확한 분기 지점을 찾는다.
5. 이미 적용된 패치는 유지하면서, 최소 변경으로 `UpdateType 0` 정상 완료를 만드는 것을 우선 목표로 한다.
6. HTTP error 우회 패치는 실제로 어떤 실행 경로에서 필요한지 별도로 분리해서 판단한다.

## 패치 원칙
- 무조건적인 함수 패치보다 실제 반환값 소비 지점을 우선 확인한다.
- Ghidra RVA와 ELF raw file offset을 혼동하지 않는다. 실제 패치 전에는 PT_LOAD 매핑을 확인한다.
- `FUN_00EE3CBC`, `FUN_00EB710C` 등 일반 런타임/초기화 helper는 업데이트 로직으로 오인하여 패치하지 않는다.
- FDE/unwind XREF는 실제 호출자로 간주하지 않는다.
- `Response_Allin1` 생성자/JSON 역직렬화 자체는 현재 패치 대상이 아니다.
