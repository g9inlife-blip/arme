# 2026-09-03 Patch 작업중

## 개발 기준
- 프로젝트: 아르메블랑쉐
- Repository: `g9inlife-blip/arme`
- 목표: 온라인 시작/업데이트 흐름을 오프라인 동작으로 전환.
- 현재 핵심 대상은 `GameUpdateService`의 시작/리소스 업데이트 판정 및 `AssetDownloader` 계층.
- 기존에 효과가 확인된 `noNeed` 관련 처리는 유지하고 다시 변경하지 않는다.
- 현재 바이너리 패치가 확실히 적용되어 있는 상태를 기준으로 분석한다.
- 무작정 성공 flag를 강제하는 패치보다 실제 실패 경로를 먼저 추적한다.

## 현재 실행 증상
```text
기존 APK
업데이트 확인중
 -> 업데이트 실패

HTTP error branch 패치 후
업데이트 확인중
 -> 리소스파일 업데이트
 -> progress 0
 -> 업데이트 실패

RequestDownload cached branch 진단 패치까지 적용 후
업데이트 확인중
 -> 리소스파일 업데이트
 -> progress 0
 -> 즉시 업데이트 실패
```

해석:
- HTTP server check가 최초 최종 병목은 아니다.
- `RequestDownload`의 cached branch를 강제로 타면 대기/queue 경로가 줄어든다.
- 그래도 즉시 실패하므로 `RequestDownload` 이후 callback/집계/상위 실패 판정을 계속 추적해야 한다.

---

# 1. 핵심 함수 주소

## GameUpdateService
- `0x01780540` `StartCheckAssets`
- `0x017805D8` `CheckAssets`
- `0x017806D0` `isPackNeedUpdate`
- `0x01780754` `isAssetNeedUpdate_OnlyCheckVerion`
- `0x01780868` `isAssetNeedUpdate_CheckCacheAndBuild`
- `0x0177F2E4` `GetAssetNeedUpdate`
- `0x0177F3C4` `DownloadUpdate`
- `0x0177EF8C` `SaveLatestVersion`
- `0x01781DF0` `GameUpdateService.d__75::MoveNext` — CheckAssets coroutine
- `0x017838D4` `GameUpdateService.<>d__60::MoveNext` — asset download/update coroutine
- `0x01784218` `GameUpdateService.d__54::MoveNext` — VersionCheckUpdate coroutine
- `GameUpdateService.<>d__66::MoveNext` — OnUpdateSuccessIE coroutine

## AssetDownloader
- `0x01847190` `AssetDownloader::RequestDownload`
- `AssetDownloader::IsCached`
- `AssetDownloader.<>d__19::MoveNext`
- `AssetDownloader.<>d__19::<>m__Finally1`
- `AssetDownloader.<>c__DisplayClass19_1::<>b__0`
- `AssetDownloader::GetCachePath`
- `AssetDownloader::Awake`

## AssetVersionManager
- `AssetVersionManager::UpdateAvailable`
- `AssetVersionManager::GetCachedVersion`
- `AssetVersionManager::Store`
- `AssetVersionManager::get_LatestVersion`
- `AssetVersionManager::set_LatestVersion`
- `AssetVersionManager::get_AssetVersionSaved`
- `AssetVersionManager::set_AssetVersionSaved`
- `AssetVersionManager::DownloadAssetIndex`
- `AssetVersionManager::UpdateVersion`
- `AssetVersionManager::LoadIndexVersion`
- `AssetVersionManager::SaveIndexVersion`

---

# 2. UpdateType 흐름

`CheckAssets` 성공 callback에서 결과를 `GameUpdateService +0x78`에 저장한다.

- `UpdateType == 0`: 업데이트 없음 / 정상 완료
- `UpdateType == 1`: Pack/버전 저장 경로
- `UpdateType == 2`: 리소스/AssetBundle 업데이트 경로

`GameUpdateService.<>d__66::MoveNext`에서 `+0x78 == 2`이면 `StringLiteral_10881`과 함께 리소스 업데이트 단계로 진입한다. 따라서 화면의 **"리소스파일 업데이트"**는 UpdateType 2와 직접 연결된다.

---

# 3. UpdateType 2 판정

`isAssetNeedUpdate_CheckCacheAndBuild` (`0x01780868`)의 native 기준은 다음 중 하나라도 true이면 업데이트 필요다.

```text
CacheManifestLocation.UpdateAvailable()
OR CacheAssetBundleInfoLocation.UpdateAvailable()
OR BuildVersion > BuildVersionSaved
OR remoteAssetVersion != AssetVersionSaved
```

단, `GameUpdateService_TypeInfo + 0xb8 + 0x160`에 Lua delegate가 있으면 native 판정보다 delegate 결과가 우선된다.

`isAssetNeedUpdate_OnlyCheckVerion` (`0x01780754`) 역시 `GameUpdateService_TypeInfo +0xb8 +0x158`의 Lua delegate override가 존재할 수 있다.

**다음 판정 추적 시 반드시 +0x158 / +0x160 delegate 존재 여부를 확인한다.**

---

# 4. VersionCheckUpdate HTTP error branch 패치

원본:
```asm
01784378  40 03 00 36    tbz w0,#0,LAB_017843e0
0178437c  73 12 40 f9    ldr x19,[x19,#0x20]
```

현재 패치:
```asm
0178437c  c3 00 00 14    b 0x01784688
```

목적:
- HTTP error branch에서 실패 callback으로 바로 내려가지 않고 success callback(UpdateType 0)을 시도.

실행 결과:
- HTTP 단계 이후 `리소스파일 업데이트`까지 진입.
- 이후 progress 0에서 실패.
- 따라서 HTTP server check는 현재 최종 실패 원인이 아니다.

---

# 5. Response_Allin1 구조

```csharp
public class Response_Allin1 : IDisposable {
    public int Status; // +0x10
    public NoticeState NoticeStatus; // +0x14
    public string NoticeTime; // +0x18
    public bool IsUpgrade; // +0x20
    public List<Response_Allin1.AssetCDN> Assets; // +0x28
    public List<Response_Allin1.KeyUrl> Keys; // +0x30
    public List<Response_Allin1.Server> Servers; // +0x38
    public Dictionary<string,string> Http_API_URL; // +0x40
    public Version AssetVersion; // +0x48
    public Response_Allin1.AssetCDN BestCDN; // +0x50
}
```

`VersionCheckUpdate` (`0x01784218`) 흐름:
1. `AppConst::get_Mark()`
2. `ProtocolGame_HttpRequest::V3_POST_AllInOne(mark,0)`
3. `IsDone` 대기
4. `isError` 검사
5. `downloadHandler.text`
6. `Response_Allin1::GetByJson`
7. `GamePlayerInfomation::set_ServerVersionManifest`
8. CheckAssets 성공 callback

정상 response에서 `BestCDN` 또는 Assets에서 remote asset version을 얻고 `AssetVersionManager::set_LatestVersion()`에 저장한다.

---

# 6. AssetVersionManager 버전 구조

## LatestVersion

`AssetVersionManager::get_LatestVersion` 분석 결과 static field 영역:
```text
AssetVersionManager_TypeInfo + 0xb8 -> static fields
static +0x00 = LatestVersion (System.Version)
```

동작:
```csharp
get_LatestVersion() {
    if (LatestVersion == null)
        LatestVersion = new Version(get_AssetVersionSaved());
    return LatestVersion;
}

set_LatestVersion(v) {
    LatestVersion = v;
}
```

## AssetVersionSaved

`get_AssetVersionSaved`:
```csharp
PlayerPrefs.GetString("AssetVersionSaved", "0.0.0.0")
```

확인된 문자열 literal:
- `StringLiteral_10591` = `"AssetVersionSaved"`
- `StringLiteral_4554` = `"0.0.0.0"`

`set_AssetVersionSaved`:
```csharp
PlayerPrefs.SetString("AssetVersionSaved", value)
```

즉 `AssetVersionSaved`는 `.version` 파일과 별개의 PlayerPrefs 문자열이다.

---

# 7. UpdateAvailable / .version 구조

`AssetVersionManager::UpdateAvailable(location)`:
```c
cached = new Version(GetCachedVersion(location));
latest = static LatestVersion;
return cached < latest;
```

즉 bundle CRC/파일 목록을 여기서 직접 검사하지 않는다.

`GetCachedVersion(location)`:
```c
string version = "0.0.0.0";
if (location.type == 3) {
    string path = AssetDownloader.GetCachePath(location);
    string versionPath = path + ".version";
    if (File.Exists(versionPath)) {
        BinaryReader br = new BinaryReader(File.OpenRead(versionPath));
        version = br.ReadString();
        br.Dispose();
    }
}
return version;
```

**확정:** `AssetLocation.type == 3 (FileCache)`일 때만 `.version`을 읽는다.

---

# 8. AssetLocation type / cache 경로

`AssetLocation::FileCache(...)`:
```text
+0x38 = AssetType/System.Type
+0x40 = fileName 관련 값
+0x48 = directory
+0x50 = fileName
+0x58 = 2
+0x5C = 3
```

`AssetLocation::AssetBundle(...)`:
```text
+0x5C = 1
```

`AssetLocation::AssetIndex(...)`:
```text
+0x5C = 4
```

따라서 FileCache(type 3)와 AssetIndex(type 4)는 서로 다른 경로다.

`AssetBundleManager::get_AssetBundleDirectory()` native 결과:
```text
"AssetBundles/" + Utility.GetPlatformForAssetBundles()
```

Android에서는:
```text
Utility.GetPlatformForAssetBundles() = "Android"
```

따라서:
```text
AssetBundleDirectory = AssetBundles/Android
AssetBundleManifestBundleName = Android
```

`AssetInfoManager::get_CacheManifestLocation()`은 FileCache로 다음을 구성한다:
```text
[AssetDownloader.CachePath]/AssetBundles/Android/Android
```

여기에 `.version`이 붙어 UpdateAvailable이 읽는 파일은:
```text
[CachePath]/AssetBundles/Android/Android.version
```

`get_CacheAssetBundleInfoLocation()`은 FileCache이며 fileName:
```text
AssetBundleInfoFile.json
```

따라서 두 번째 `.version` 파일:
```text
[CachePath]/AssetBundles/Android/AssetBundleInfoFile.json.version
```

---

# 9. 실제 repository의 resource version 파일

`work/resource_version_folder/`에 확인된 파일:
```text
Android
Android.version
AssetBundleInfoFile.json
AssetBundleInfoFile.json.version
```

`Android.version` 및 `AssetBundleInfoFile.json.version` 내용은 사람이 읽는 순수 문자열이 아니라 BinaryWriter 문자열 형식으로 보이며:
```text
0x06 + "3.1.88"
```
형태다.

`AssetBundleInfoFile.json` 시작 내용:
```json
{"Verion":"3.1.88","AllAssetBundles":[{"assetBundleName":"2.assetbundle","fileSize":1316324,"CRC":170869396,"Version":0}, ...]}
```

따라서 이 파일들은 런타임에서 기대하는 `.version` 형식과 일치하는 강한 증거가 있다.

**주의:** repository의 `work/resource_version_folder` 파일과 실제 Android `persistentDataPath`의 cache 파일은 별개다. 실제 런타임 cache 위치는 별도로 확인해야 한다.

---

# 10. AssetDownloader CachePath

`AssetDownloader::Awake` native 경로:
```asm
018464b0 ...
018464b4  bl PlatformTools$$get_persistentDataPath
...
018464c8  b  AssetDownloader$$set_CachePath
```

즉 native 기준:
```text
AssetDownloader.CachePath = PlatformTools.get_persistentDataPath()
```

`PlatformTools::get_persistentDataPath()`:
```text
Application.persistentDataPath
```
이 비어 있으면 `CustomPersistentDataPath` fallback.

단, Awake에는 Lua override가 있을 수 있다.

`AssetDownloader::get_CachePath()` 자체에도 Lua delegate가 있다:
```text
AssetDownloader_TypeInfo +0xb8 +0x18
```

native instance field 기준 cache root는 AssetDownloader instance `+0x38`.

---

# 11. SaveLatestVersion 분석 — 중요 신규 확정 사항

함수:
```text
0x0177EF8C AliothEngine.GameUpdateService$$SaveLatestVersion
```

전체 흐름:
```c
SaveLatestVersion(service)
{
    BuildVersion = GameUpdateService.get_BuildVersion(service);
    BuildVersionBindings.set_BuildVersion(BuildVersion);

    AssetBundleInfo = AssetInfoManager.get_AssetBundle();

    if (AssetBundleInfo != null) {
        AssetVersionManager.set_AssetVersionSaved(
            *(string *)(AssetBundleInfo + 0x10)
        );
    }
    else {
        Debug.LogError(...);
    }
}
```

핵심 assembly:
```asm
0177f060 e1 03 1f aa     mov x1,xzr
0177f064 55 a1 05 94     bl  AliothEngine.AssetInfoManager$$get_AssetBundle
0177f068 00 03 00 b4     cbz x0,LAB_0177f0c8
...
0177f0b0 60 0a 40 f9     ldr x0,[x19,#0x10]
...
0177f0bc e1 03 1f aa     mov x1,xzr
0177f0c4 4a b7 05 14     b   AliothEngine.AssetVersionManager$$set_AssetVersionSaved
```

즉 정확히:
```text
AssetInfoManager.get_AssetBundle()
    -> returned object +0x10
    -> PlayerPrefs.SetString("AssetVersionSaved", value)
```

### SaveLatestVersion 자체의 Lua override
함수 시작 부분에서:
```asm
0177efc4 ...
0177efcc ldr x8,[GameUpdateService_TypeInfo]
0177efd0 ldr x8,[x8,#0xb8]
0177efd4 ldr x0,[x8,#0xa0]
0177efd8 cbz x0,LAB_0177eff4
0177eff0 b XLua.DelegateBridge$$__Gen_Delegate_Imp23
```

따라서:
```text
GameUpdateService_TypeInfo +0xb8 +0xa0
```
에 Lua delegate가 있으면 native SaveLatestVersion을 건너뛸 수 있다.

---

# 12. SaveLatestVersion 호출 시점 확정

`GameUpdateService.<>d__66::MoveNext`에서 `SaveLatestVersion()` 호출이 확인되었다.

핵심 구조:
```c
if (state == 0) {
    if (service.UpdateType == 2) {
        service.UpdateStepDescription = StringLiteral_10881;
        service.UpdateSteping = 7;
        state = 1;
        return 1; // yield
    }
    ...
}

if (state == 1) {
    SaveLatestVersion(service);
    if (service.OnUpdateSuccess != null)
        service.OnUpdateSuccess.Invoke();
    return 0;
}
```

따라서 **SaveLatestVersion은 UpdateType 2가 발생하기 전에 호출되는 초기화 함수가 아니다.**

정확한 의미:
```text
UpdateType 2 결정
    ↓
실제 리소스 업데이트
    ↓
업데이트 성공 후 d66 재개
    ↓
SaveLatestVersion()
    ↓
AssetVersionSaved 저장
    ↓
OnUpdateSuccess
```

### 중요한 결론
`AssetVersionSaved`가 초기 실행에서 최신 버전과 달라서 UpdateType 2가 발생하는 경우, `SaveLatestVersion()`은 그 최초 판정을 해결하지 않는다.

오히려 `SaveLatestVersion`은 **업데이트 성공을 완료한 뒤 다음 실행을 위해 PlayerPrefs를 최신 버전으로 저장하는 후처리**다.

---

# 13. AssetInfoManager::get_AssetBundle 관련 정정

Ghidra에서 실제 함수가 확인되었다:
```text
0x018E75B8 AliothEngine.AssetInfoManager$$get_AssetBundle
```

함수 시작:
```asm
018e75b8 f4 4f be a9
018e75bc fd 7b 01 a9
018e75c0 fd 43 00 91
018e75c4 f4 33 01 b0
018e75c8 88 be 7c 39
018e75cc f3 03 00 aa
018e75d0 e8 00 00 37
018e75d4 ...
018e75e0 cb 3e d7 97    bl FUN_00eb710c
018e75e4 e8 03 00 32
018e75e8 88 be 3c 39
```

현재 확보된 것은 함수 앞부분뿐이며 **반환 필드까지는 아직 확인하지 않았다.**

따라서 현재 확정적으로 말할 수 있는 것은:
```text
SaveLatestVersion
 -> get_AssetBundle() [0x018E75B8]
 -> returned object +0x10
 -> SetAssetVersionSaved()
```

`get_AssetBundleInfos()`와 동일 객체라고 아직 단정하지 않는다.

별도로 확인된:
```text
AssetInfoManager::get_AssetBundleInfos()
    native return = *(param_1 + 0x30)
```

즉 `AssetInfoManager +0x30`은 `AssetBundleInfos`이며, 이것과 `get_AssetBundle()` 반환 객체의 동일성은 추가 확인이 필요하다.

---

# 14. AssetVersionManager::Store

`AssetDownloader.<>c__DisplayClass19_1::<>b__0`에서 실제 다운로드 후:
```c
AssetVersionManager.Store(bytes, location);
```
를 호출한다.

Store는 다음 작업을 한다:
```text
GetCachePath(location)
 -> <cache file>.tmp 작성
 -> 기존 cache file 삭제
 -> tmp를 실제 cache file로 Move
 -> <cache file>.version.tmp 작성
 -> BinaryWriter.Write(LatestVersion 관련 문자열)
 -> BinaryWriter dispose
 -> tmp를 <cache file>.version으로 Move
```

따라서 정상 다운로드 후 `.version`은 `AssetVersionManager::Store()`가 생성/갱신한다.

`GetCachedVersion()`이 `.version`을 `BinaryReader.ReadString()`으로 읽는 것과 형식이 일치한다.

---

# 15. AssetDownloader::RequestDownload

함수:
```text
0x01847190
```

인자:
- `x0` = AssetDownloader
- `x1` = AssetLocation
- `x2` = Action<bool> callback
- `x3` = 0

native cached 분기:
```asm
018473E8 e0 03 13 aa
018473EC 9d fd ff 97    bl AssetDownloader$$IsCached
018473F0 20 06 00 36    tbz w0,#0,LAB_018474B4
```

cached:
```asm
01847408 e1 03 00 32    mov w1,#1
0184740C e0 03 15 aa
01847414 67 01 4c 94    bl System.Action<bool>$$Invoke
```

즉 `IsCached == true`이면 실제 다운로드 queue/context 생성 없이 callback(true)을 즉시 호출한다.

Lua override:
```text
AssetDownloader_TypeInfo +0xb8 +0x70
```

해당 delegate가 존재하면 native RequestDownload을 건너뛸 수 있다.

---

# 16. RequestDownload 캐시 분기 진단 패치

원본:
```asm
018473F0 20 06 00 36    tbz w0,#0,LAB_018474B4
```

진단 패치:
```asm
018473F0 04 00 00 14    b 0x01847400
```

효과:
- 모든 location을 cached로 취급
- callback(true) 경로로 이동
- 실제 다운로드 queue/context 경로를 건너뜀

실행 결과:
- 실패까지의 대기 시간이 줄어듦.
- 그래도 `리소스파일 업데이트 -> progress 0 -> 즉시 실패`.

결론:
- 이 패치는 최종 해결책이 아니다.
- RequestDownload 내부 대기/queue가 현재 최종 실패 원인이라는 증거가 아니다.
- **callback 이후의 상위 집계/실패 판정을 확인해야 한다.**

현재 이 패치는 진단용으로만 기록하며 반복 변형하지 않는다.

---

# 17. AssetDownloader::IsCached

확정 native 의미:
```c
if (AssetLocation.type == 1)
    return AssetBundleManager.IsCached(fileName);

if (AssetLocation.type == 2)
    return true;

cachePath = AssetDownloader.GetCachePath(location);
return File.Exists(cachePath) && FileInfo(cachePath).Length > 0;
```

Lua override:
```text
AssetDownloader_TypeInfo +0xb8 +0x48
```

따라서 `IsCached()` 전역 true 패치는 하지 않는다.

---

# 18. AssetDownloadingContext / d19 실제 성공 경로

Context fields:
```text
+0x10 = Action<bool> OnFinish
+0x18 = bool finished
+0x2c = progress
+0x30 = error
```

`AssetDownloader.<>d__19::MoveNext`의 정상 흐름:
```text
WWW 다운로드
 -> error 없음
 -> get_bytes()
 -> download success flag (+0x29)
 -> ThreadPool callback
 -> AssetVersionManager::Store()
 -> Store 성공 flag (+0x18)
 -> closure19_0 +0x18 = downloadSuccess && storeSuccess
 -> Finally1
 -> AssetDownloadingContext::Finish(context, bool)
```

`<>m__Finally1`에서:
```c
Finish(context, closure19_0 + 0x18)
```
가 호출된다.

따라서 native 실제 다운로드 경로에서는:
```text
WWW 성공만으로 최종 성공 확정 X
Store 성공도 필요
```
다.

---

# 19. d60 completion flag 패치 — 실패한 접근

다음 위치를 강제로 성공 처리하는 패치를 적용/검토했으나 해결되지 않았다:
```text
0x01783A14
0x01783A68
```

따라서 d60 completion flag는 다시 패치하지 않는다.

---

# 20. GetAssetbundlesNeedsUpdate

`GetAssetbundlesNeedsUpdate()`는 AssetBundleManifest에서 모든 bundle을 얻고 `IsCached(name)`이 false인 bundle만 리스트에 추가한다.

진단 패치:
```asm
0x0120EC18
원본: c0 0a 00 37    tbnz w0,#0,LAB_0120ed70
패치: 16 00 00 14    b LAB_0120ed70
```

효과:
- bundle이 cached인지 여부와 무관하게 skip하여 빈 update list를 유도.

실행 결과:
- 문제 해결되지 않음.

따라서 이 패치는 최종 해결책으로 채택하지 않는다.
`IsCached` 전역 수정도 하지 않는다.

---

# 21. GameUpdateService d50 / asset update orchestration

`GameUpdateService.<>d__50::MoveNext`:
```text
state 0
 -> service ready 확인
 -> service +0x70의 update list/context 확인
 -> 비어 있으면 OnUpdateSuccess
 -> 아니면 state 2로 yield
state 2
 -> UpdateSteping = 6
 -> yield state 3
state 3
 -> StartUpdateAssetBundle(service)
 -> coroutine 시작
state 4
 -> complete
```

`StartGetAssetNeedUpdate` / `GetAssetNeedUpdate` / d55는 AssetBundle host domain을 확인하고 `GetAssetbundlesNeedsUpdate()`를 호출한다.

`StartGetAssetNeedUpdate`에는 Lua delegate가 존재할 수 있다:
```text
GameUpdateService_TypeInfo +0xb8 +0xd0
```

`GetAssetNeedUpdate`에도 Lua override가 존재할 수 있다.

---

# 22. AssetVersionManager DownloadAssetIndex / UpdateVersion

`AssetVersionManager::DownloadAssetIndex`는 `AssetLocation::AssetIndex(...)`를 생성하고 최종적으로:
```asm
... bl AliothEngine.AssetLoader$$LoadAsset
```
를 호출한다.

중요:
- AssetIndex는 `AssetLocation.type == 4`
- `.version`을 직접 읽는 FileCache(type 3)와 다른 경로다.

`UpdateVersion`은:
```text
LoadIndexVersion(info)
 -> currentIndex = info +0x18
 -> currentIndex > savedIndex 검사
 -> Dictionary(this +0x20) Contains 검사
 -> 필요 시 DownloadAssetIndex / update path
```

`LoadIndexVersion` / `SaveIndexVersion`은 PlayerPrefs int를 사용하며 `.version`과 별개다.

---

# 23. 현재 가장 중요한 신규 결론

### SaveLatestVersion은 원인이 아니다.

정확히:
```text
UpdateType 2 발생
    ↓
실제 리소스 업데이트
    ↓
성공
    ↓
SaveLatestVersion
    ↓
AssetVersionSaved 갱신
```

따라서 최초 실행에서:
```text
AssetVersionSaved != remoteAssetVersion
```
때문에 UpdateType 2가 발생하더라도 `SaveLatestVersion`을 앞단에서 고치는 것은 논리적으로 맞지 않는다.

### 현재 핵심 미확정점
1. `isAssetNeedUpdate_CheckCacheAndBuild`의 Lua delegate `+0x160` 실제 존재 여부/반환값
2. `isAssetNeedUpdate_OnlyCheckVerion`의 Lua delegate `+0x158` 존재 여부
3. 실제 런타임의 두 `.version` cache 상태
4. `AssetInfoManager::get_AssetBundle()` 반환 객체의 정확한 타입 및 `+0x10` 필드
5. `DownloadUpdate`가 `RequestDownload`에 전달하는 `Action<bool>`의 실제 callback method body
6. callback 결과가 여러 asset의 성공/실패를 어떻게 집계하여 `d60`/상위 `OnUpdateFail`로 연결하는지

---

# 24. 다음 분석 순서

## 1순위 — RequestDownload callback 실제 method body
`DownloadUpdate`의:
```asm
0177f5c0  mov x1,x19
0177f5c4  mov x22,x0
0177f5c8  ldr x2,[x8]
0177f5cc  ldr x3,[x9]
0177f5d0  bl System.Action<bool>::.ctor
...
0177f5e8  mov x2,x22
0177f5f0  bl AssetDownloader$$RequestDownload
```
에서 생성된 `Action<bool>`의 실제 method body를 DisplayClass XREF로 찾는다.

특히 cached branch에서 `callback(true)`를 호출했을 때 어떤 코드가 실행되고 최종적으로 왜 failure가 되는지 확인한다.

## 2순위 — isAssetNeedUpdate delegate 확인
```text
GameUpdateService_TypeInfo +0xb8 +0x158
GameUpdateService_TypeInfo +0xb8 +0x160
```
의 delegate 존재 여부를 확인한다.

## 3순위 — get_AssetBundle 반환 구조
`0x018E75B8 get_AssetBundle`을 끝까지 확인하여 반환 필드/객체 타입을 확정한다.

## 4순위 — Store 실패 조건
`AssetVersionManager::Store`의 실제 파일 경로/예외/Move 실패 조건을 추가 확인한다.

## 5순위 — 실제 런타임 cache 경로
native 기준:
```text
persistentDataPath/AssetBundles/Android/Android.version
persistentDataPath/AssetBundles/Android/AssetBundleInfoFile.json.version
```
가 존재하고 올바른 BinaryWriter 문자열을 갖는지 확인한다.

---

# 25. 패치 원칙

- 이미 효과가 확인된 패치는 유지하고 불필요하게 다시 변경하지 않는다.
- `0x0178437C` HTTP error branch 우회는 현재 분석 기준에서 유지한다.
- `d60` completion flag 강제 패치는 재적용하지 않는다.
- `AssetDownloader::IsCached()` 전역 true 패치는 하지 않는다.
- `AssetDownloadingContext::Finish()` 자체는 패치하지 않는다.
- `0x018473F0` RequestDownload cached 강제는 진단용으로만 취급한다.
- `GetAssetbundlesNeedsUpdate` 강제 skip 패치는 최종 해결책으로 채택하지 않는다.
- 실제 callback/Store/UpdateType 판정 원인을 확인한 후 최소 범위로 패치한다.

## 현재 작업 기준
**다음 작업은 패치가 아니라 `RequestDownload -> Action<bool> callback -> 상위 성공/실패 집계`의 실제 코드를 추적하는 것부터 시작한다.**
