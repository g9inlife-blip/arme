# 2026-09-03 Patch 작업중

## 개발 기준
- 프로젝트: 아르메블랑쉐
- Repository: `g9inlife-blip/arme`
- 목표: 온라인 시작/업데이트 흐름을 오프라인 동작으로 전환.
- 현재 핵심 대상은 `GameUpdateService`의 시작/리소스 업데이트 판정 및 AssetDownloader 계층.
- 기존에 효과가 확인된 `noNeed` 관련 처리는 유지하고 다시 변경하지 않는다.
- 현재 바이너리 패치가 확실히 적용되어 있는 상태를 기준으로 분석한다.
- 무작정 성공 flag를 강제하는 패치보다 실제 실패 경로를 먼저 추적한다.

## 현재 확인된 핵심 함수

### GameUpdateService
- `0x01780540` `StartCheckAssets`
- `0x017805D8` `CheckAssets`
- `0x017806D0` `isPackNeedUpdate`
- `0x01780754` `isAssetNeedUpdate_OnlyCheckVerion`
- `0x01780868` `isAssetNeedUpdate_CheckCacheAndBuild`
- `0x0177F2E4` `GetAssetNeedUpdate`
- `0x0177F3C4` `DownloadUpdate`
- `0x01781DF0` `GameUpdateService.d__75::MoveNext` — CheckAssets coroutine
- `0x017838D4` `GameUpdateService.<>d__60::MoveNext` — asset download/update coroutine
- `0x01784218` `GameUpdateService.d__54::MoveNext` — VersionCheckUpdate coroutine
- `0x01784...` `GameUpdateService.<>d__66::MoveNext` — OnUpdateSuccessIE coroutine

### AssetDownloader
- `0x01847190` `AliothEngine.AssetDownloader$$RequestDownload`
- `AliothEngine.AssetDownloader$$IsCached`
- `AliothEngine.AssetDownloader.<>d__19::MoveNext`
- `AliothEngine.AssetDownloader.<>d__19::<>m__Finally1`
- `AliothEngine.AssetDownloader.<>c__DisplayClass19_1::<>b__0`

## UpdateType 흐름
`CheckAssets` 성공 callback에서 서버/로컬 판정 결과를 `GameUpdateService +0x78`에 저장한다.

- `UpdateType == 0`: 업데이트 없음 / 정상 완료 경로
- `UpdateType == 1`: 버전 저장 경로
- `UpdateType == 2`: 리소스/AssetBundle 업데이트 경로

`GameUpdateService.<>c__DisplayClass75_0::<CheckAssets>b__0`은 UpdateType을 `+0x78`에 저장하고 로그를 출력한다.

`GameUpdateService.<>d__66::MoveNext`에서 `+0x78 == 2`이면 `StringLiteral_10881`과 함께 리소스 업데이트 단계로 진입한다. 따라서 사용자에게 보이는 **"리소스파일 업데이트"**는 UpdateType 2 경로와 직접 연결된다.

## 세 가지 업데이트 판정 함수

### 1. isPackNeedUpdate — `0x017806D0`
서버 `Response_Allin1.IsUpgrade` (`+0x20`)를 반환한다. Lua delegate가 있으면 delegate 결과를 사용한다.

### 2. isAssetNeedUpdate_OnlyCheckVerion — `0x01780754`
버전 비교 기반 Asset 업데이트 필요 여부를 판단한다.

### 3. isAssetNeedUpdate_CheckCacheAndBuild — `0x01780868`
확인된 조건:
- CacheManifest `UpdateAvailable`
- CacheAssetBundleInfo `UpdateAvailable`
- `BuildVersion > BuildVersionBindings`
- `remoteAssetVersion != AssetVersionSaved`
중 하나라도 true면 업데이트 필요로 반환한다.
서비스 TypeInfo `+0x160`에 Lua delegate가 있으면 delegate 결과가 우선된다.

## VersionCheckUpdate HTTP error branch 패치
`0x01784378`의 `isError` 검사 자체는 유지하고, error branch에서 `x19`을 덮어쓰는 명령을 success 경로로 우회하는 방식으로 패치했다.

원본:
```asm
0178437c 73 12 40 f9    ldr x19,[x19,#0x20]
```

패치:
```asm
0178437c c3 00 00 14    b 0x01784688
```

목적:
- HTTP error 발생 시 실패 callback으로 바로 내려가지 않고 success callback(UpdateType 0)을 호출하는 경로를 시도.

실행 결과:
- HTTP 단계는 우회되어 이후 **"리소스파일 업데이트"**까지 진입.
- 그러나 progress 0 이후 **"업데이트 실패"**가 발생.
- 따라서 HTTP server check가 현재 최종 병목은 아니다.

## VersionCheckUpdate 핵심 흐름
`0x01784218` `d__54::MoveNext`:
1. `AppConst::get_Mark()`
2. `ProtocolGame_HttpRequest::V3_POST_AllInOne(mark,0)`
3. `IsDone` 대기
4. `isError` 검사
5. 정상 응답이면 `downloadHandler.text`
6. `Response_Allin1::GetByJson(text)`
7. `GamePlayerInfomation::set_ServerVersionManifest(...)`
8. CheckAssets 성공 callback으로 UpdateType 전달

## Response_Allin1 구조
```csharp
public class Response_Allin1 : IDisposable {
    public int Status; // 0x10
    public NoticeState NoticeStatus; // 0x14
    public string NoticeTime; // 0x18
    public bool IsUpgrade; // 0x20
    public List<Response_Allin1.AssetCDN> Assets; // 0x28
    public List<Response_Allin1.KeyUrl> Keys; // 0x30
    public List<Response_Allin1.Server> Servers; // 0x38
    public Dictionary<string,string> Http_API_URL; // 0x40
    public Version AssetVersion; // 0x48
    public Response_Allin1.AssetCDN BestCDN; // 0x50
}
```

`GetByJson`은 기본적으로 `JsonUtility::FromJson<Response_Allin1>`을 사용한다. `OnAfterDeserialize`는 주로 `Http_API_URL` dictionary 구성 및 hotfix delegate 처리이며 현재 직접 패치 대상이 아니다.

## OnUpdate 관련 함수
- `OnAssetUpdate`: `+0x58` completion callback invoke
- `OnPackUpdate`: `+0x50` completion callback invoke
- `OnUpdateFail`: `+0x60` failure callback invoke 및 오류 문자열 설정
- `OnUpdateSuccess`: `OnUpdateSuccessIE` coroutine 시작

## GameUpdateService DownloadUpdate 흐름
`0x0177F3C4` `DownloadUpdate`는 Lua delegate override가 있으면 native 경로 대신 delegate를 호출한다.

native 경로에서 Transform 목록을 AssetLocation/JSONNode 목록으로 변환하고 AssetDownloader Singleton을 얻은 뒤 `Action<bool>` callback을 생성하여 `RequestDownload`에 전달한다.

확인된 listing:
```asm
0177f588 68 30 01 d0    adrp x8,0x3d8d000
0177f58c 08 ad 41 f9    ldr  x8,[x8,#0x358]
0177f590 00 01 40 f9    ldr  x0,[x8]
0177f594 54 ff 47 94    bl   AliothEngine.SingletonBehaviour<>$$get_Instance
0177f598 c8 2b 01 b0    adrp x8,0x3cf8000
0177f59c 08 11 45 f9    ldr  x8,[x8,#0xa20]
0177f5a0 f4 03 00 aa    mov  x20,x0
0177f5a4 08 01 40 f9    ldr  x8,[x8]
0177f5a8 e0 03 08 aa    mov  x0,x8
0177f5ac af cd dd 97    bl   thunk_FUN_00ef2ff4
0177f5b0 e8 2d 01 b0    adrp x8,0x3d3c000
0177f5b4 49 2e 01 f0    adrp x9,0x3d4a000
0177f5b8 08 75 43 f9    ldr  x8,[x8,#0x6e8]
0177f5bc 29 3d 40 f9    ldr  x9,[x9,#...]
0177f5c0 e1 03 13 aa    mov  x1,x19
0177f5c4 f6 03 00 aa    mov  x22,x0
0177f5c8 02 01 40 f9    ldr  x2,[x8]
0177f5cc 23 01 40 f9    ldr  x3,[x9]
0177f5d0 f4 20 4f 94    bl   System.Action<bool>::.ctor
0177f5e0 e0 03 14 aa    mov  x0,x20
0177f5e4 e1 03 15 aa    mov  x1,x21
0177f5e8 e2 03 16 aa    mov  x2,x22
0177f5ec e3 03 1f aa    mov  x3,xzr
0177f5f0 c3 20 03 94    bl   AliothEngine.AssetDownloader$$RequestDownload
```

### Delegate 구조 해석
- `x20` = AssetDownloader Singleton
- `x21` = AssetLocation
- `x22` = 새로 생성된 `Action<bool>`
- `Action<bool>` 생성 시 target은 `x19`의 GameUpdateService이고 MethodInfo를 통해 `GameUpdateService.<>c__DisplayClass...` 계열 callback에 연결된다.
- `03d3c6e8`은 `03f1ce40`을 가리키는 `PTR_Method$...`로 확인되었다.
- `03f1ce40`은 Ghidra가 만든 `Method$...` 메모리/심볼 영역으로, 실제 ELF hex에서 바이트가 존재하지 않아 `??`로 표시된다.
- 따라서 `03f1ce40`의 raw hex를 추적하는 방향은 중단한다. 실제 callback은 DownloadUpdate의 delegate 생성/DisplayClass XREF를 통해 찾는 것이 맞다.

## AssetDownloader::RequestDownload
### 함수
- `0x01847190` `AliothEngine.AssetDownloader$$RequestDownload`
- 인자:
  - `x0` = AssetDownloader instance
  - `x1` = AssetLocation
  - `x2` = Action<bool> callback
  - `x3` = 0

### Lua override
```asm
01847200 08 5c 40 f9    ldr x8,[x0,#0xb8]
01847204 00 39 40 f9    ldr x0,[x8,#0x70]
01847208 80 01 00 b4    cbz x0,LAB_01847238
...
01847234 a7 19 e4 17    bl XLua.DelegateBridge$$__Gen_Delegate_Imp803
```

해당 Lua delegate가 존재하면 native RequestDownload 구현을 건너뛴다.

### Native 흐름
1. `AssetLocation::get_url`
2. URL 로그
3. `AssetLocation::get_hash`
4. `AssetDownloader +0x20` dictionary에서 hash로 기존 context 검색
5. 기존 context가 있으면 priority 비교 후 callback 연결
6. 없으면 `AssetDownloadingContext` 생성
7. `OnFailure` callback 연결
8. 다운로드 queue/context에 추가
9. global `AssetDownloadingContext` 상태 반환

### 캐시 분기
```asm
018473E8 e0 03 13 aa    mov x0,x19
018473EC 9d fd ff 97    bl AliothEngine.AssetDownloader$$IsCached
018473F0 20 06 00 36    tbz w0,#0,LAB_018474B4
```

cached 경로:
```asm
01847408 e1 03 00 32    mov w1,#1
0184740C e0 03 15 aa    mov x0,x21
01847414 67 01 4c 94    bl System.Action<bool>$$Invoke
```

즉 `IsCached == true`이면 실제 다운로드/컨텍스트 생성 없이 callback(true)을 즉시 호출한다.

## RequestDownload 캐시 분기 진단 패치
원본:
```asm
018473F0 20 06 00 36    tbz w0,#0,LAB_018474B4
```

진단 패치:
```asm
018473F0 04 00 00 14    b 0x01847400
```

효과:
- 모든 AssetLocation을 cached로 취급
- `Action<bool>`에 true 전달
- 실제 AssetDownloadingContext 생성/다운로드 queue 경로를 건너뜀

실행 결과:
- 이전보다 실패까지의 대기 시간이 줄어듦.
- **"리소스파일 업데이트" → progress 0 → 즉시 업데이트 실패**.
- 따라서 이 패치는 RequestDownload 내부의 대기/실제 다운로드 queue 경로를 우회하고 있음을 시사하지만, 상위 단계의 최종 실패 판정은 여전히 남아 있다.

중요: 이 패치는 최종 해결책이 아니라 진단용이며, 현재는 반복 적용/추가 변형하지 않는다.

## AssetDownloader::IsCached 분석
사용자가 `AliothEngine.AssetDownloader$$IsCached` 전체 decompile을 제공했다.

핵심 로직:
```c
int type = AssetLocation::get_type(param_1);
if (type != 1) {
    type = AssetLocation::get_type(param_1);
    if (type != 2) {
        ...
        string cachePath = AssetDownloader::GetCachePath(param_1);
        if (File::Exists(cachePath)) {
            FileInfo info(cachePath);
            return info.Length > 0;
        }
        return 0;
    }
    return 1;
}
string fileName = AssetLocation::get_fileName(param_1);
return AssetBundleManager::IsCached(fileName);
```

확정된 의미:
- `AssetLocation.type == 1` → `AssetBundleManager::IsCached(fileName)` 결과
- `AssetLocation.type == 2` → **무조건 true**
- 그 외 type → `GetCachePath()` 후 `File.Exists()` 및 `FileInfo.Length > 0`
- 함수 초반 `AssetDownloader_TypeInfo` 계열 `+0xb8 +0x48`에 Lua delegate가 있으면 delegate 결과를 사용한다.

따라서 `IsCached()` 자체를 전역적으로 true로 패치할 이유가 없다. 특히 type 2는 원래부터 cached 처리되고, type 1만 AssetBundleManager의 실제 캐시 판정을 거친다.

## AssetDownloadingContext 및 실제 Finish 경로
확인된 fields:
- `+0x10` = `Action<bool> OnFinish`
- `+0x18` = `bool finished`
- `+0x2c` = progress
- `+0x30` = error

`AssetDownloadingContext::Finish(context,bool)`은 Lua override가 없으면 `OnFinish.Invoke(bool)` 후 finished를 true로 설정한다.

### d__19::MoveNext
`AliothEngine.AssetDownloader.<>d__19::MoveNext`는 실제 WWW 다운로드와 background Store를 수행한다.

주요 state machine fields:
- `+0x10` = state
- `+0x18` = wait/coroutine object
- `+0x28` = AssetDownloader
- `+0x30` = `<>c__DisplayClass19_1`
- `+0x38` = `<>c__DisplayClass19_0`
- `+0x40` = cache path
- `+0x48` = URL/parameters
- `+0x50` = RetryController
- `+0x58` = WWW

closure19_1 주요 fields:
- `+0x10` = downloaded bytes
- `+0x18` = Store 성공 flag
- `+0x20` = result/error 관련 값
- `+0x28` = background processing 완료 flag
- `+0x29` = WWW 다운로드 성공 flag
- `+0x30` = closure19_0 참조

정상 흐름:
```text
WWW 생성
 ↓
WWW.isDone 대기
 ↓
WWW.error 확인
 ↓
error 없음
 ↓
AssetDownloadingContext::set_error(context,0)
 ↓
WWW.get_bytes()
 ↓
closure +0x29 = true
 ↓
ThreadPool에 ProcessDownload callback queue
 ↓
AssetVersionManager::Store(bytes, location)
 ↓
closure19_1 +0x18 = true
closure19_1 +0x28 = true
closure19_0 +0x18 = true
 ↓
<>m__Finally1
 ↓
AssetDownloadingContext::Finish(context,true)
```

실패 흐름에서는 WWW error/RetryController 결과에 따라 `+0x29`가 false로 남고 최종적으로 `Finish(false)`가 가능하다.

### DisplayClass19_1::<>b__0
사용자가 제공한 decompile:
```c
void <>b__0(long param_1)
{
    AssetVersionManager instance = Singleton<AssetVersionManager>::get_Instance();
    bytes = *(long *)(param_1 + 0x10);
    closure19_0 = *(long *)(param_1 + 0x30);
    context = *(long *)(closure19_0 + 0x10);
    location = AssetDownloadingContext::get_location(context);
    if (instance != 0) {
        AssetVersionManager::Store(instance, bytes, location);
        *(bool *)(param_1 + 0x18) = true;
        *(bool *)(param_1 + 0x28) = true;
        if (closure19_0 != 0) {
            *(bool *)(closure19_0 + 0x18) =
                *(char *)(param_1 + 0x29) != 0 && *(char *)(param_1 + 0x18) != 0;
            return;
        }
    }
    exception;
}
```

확정 의미:
- 실제 다운로드 성공만으로 최종 성공이 확정되는 것이 아니다.
- `AssetVersionManager::Store()`가 정상 완료되어야 `closure19_1 +0x18 = true`가 된다.
- 이후 `closure19_0 +0x18 = (+0x29 && +0x18)`가 된다.
- `AssetVersionManager` Singleton이 null이면 예외 경로다.

### d__19::<>m__Finally1
사용자가 제공한 decompile:
```c
void <>m__Finally1(long param_1)
{
    AssetDownloader downloader = *(long *)(param_1 + 0x28);
    state = -1;
    Dictionary dict = *(long *)(downloader + 0x20);
    AssetDownloadingContext context = *(long *)(*(long *)(param_1 + 0x38) + 0x10);
    string hash = AssetDownloadingContext::get_hash(context);
    Dictionary.Remove(dict, hash);
    int n = AssetDownloader::get_numProcessing(downloader);
    AssetDownloader::set_numProcessing(downloader, n - 1);
    closure19_0 = *(long *)(param_1 + 0x38);
    if (*(long *)(closure19_0 + 0x10) == 0) exception;
    AssetDownloadingContext::Finish(
        *(long *)(closure19_0 + 0x10),
        *(char *)(closure19_0 + 0x18) != 0,
        0);
}
```

이로써 **native 다운로드 경로에서 `AssetDownloadingContext::Finish()`를 호출하는 정확한 caller가 확보되었다.**

핵심:
- `Finish()` 자체를 패치할 필요가 없다.
- Finish에 전달되는 bool은 `closure19_0 +0x18`이다.
- 이 값은 다운로드 성공(`+0x29`)과 Store 성공(`+0x18`)의 AND 결과다.

## d__60 completion flag 강제 패치 — 실패한 접근
`GameUpdateService.<>d__60::MoveNext`에서 다음 위치를 강제로 성공으로 만드는 패치를 적용/검토했으나 해결되지 않았다.

```text
0x01783A14
0x01783A68
```

따라서 d60 flag를 다시 패치하지 않는다.

## 현재 실행 증상과 해석
현재 패치 상태에서:
```text
HTTP error branch 우회
    ↓
CheckAssets
    ↓
UpdateType == 2
    ↓
"리소스파일 업데이트"
    ↓
progress 0
    ↓
RequestDownload 캐시 분기 진단 패치 시
    ↓
즉시 업데이트 실패
```

확정/강한 추정:
1. HTTP server check 자체는 최초 병목이 아니다.
2. `RequestDownload`의 cached branch를 강제하면 실제 다운로드 queue/대기 경로가 줄어든다.
3. 그 상태에서도 즉시 실패하므로 **RequestDownload 이후의 GameUpdateService callback/집계/상위 실패 판정**이 매우 중요한 다음 추적 대상이다.
4. 정상적인 실제 다운로드 경로를 복구할 경우에는 `AssetVersionManager::Store()`가 `Finish(true)`까지 이어지는 핵심 조건이다.
5. 그러나 현재 cached-branch 진단에서는 d19가 호출되지 않을 수 있으므로, 지금 즉시 실패를 설명하려면 `DownloadUpdate`가 만든 `Action<bool>` callback의 실제 method body를 먼저 확인하는 것이 가장 우선이다.

## MethodInfo 추적에 대한 정정
`DownloadUpdate`에서 `Action<bool>` 생성에 사용되는 MethodInfo 관련 주소:
- `03d3c6e8` = `PTR_Method$AliothEngine.GameUpdateService.<>c_...`
- 내용은 `03f1ce40`을 가리킴.
- `03f1ce40`은 Ghidra가 만든/해석한 `Method$...` 영역으로 실제 ELF hex 데이터가 존재하지 않아 `??`로 표시됨.

따라서 `03f1ce40`의 raw hex를 계속 찾지 않는다.
실제 callback 함수는:
- DownloadUpdate의 `Action<bool>` 생성부
- 해당 DisplayClass의 XREF
- Ghidra Function/Symbol Tree의 `GameUpdateService.<>c__DisplayClass...` 메서드
를 이용해 찾는다.

## 다음 분석 순서
### 1순위
`GameUpdateService::DownloadUpdate`에서 생성하는 `Action<bool>`의 실제 callback method body 확인.

현재 확보된 핵심:
```asm
0177f5c0  mov x1,x19
0177f5c4  mov x22,x0
0177f5c8  ldr x2,[x8]
0177f5cc  ldr x3,[x9]
0177f5d0  bl System.Action<bool>::.ctor
...
0177f5e8  mov x2,x22
0177f5f0  bl AssetDownloader$$RequestDownload
```

여기서 `RequestDownload`의 `callback(true)`가 실제로 어떤 DisplayClass method로 들어가는지 추적한다.

### 2순위
`AssetVersionManager$$Store` 구현 분석.
- Store가 실제 파일을 어디에 기록하는지
- 예외/실패 조건
- offline에서 필요한 초기화/경로
- Store 이후 AssetBundleManager가 해당 파일을 인식하는지 확인

### 3순위
필요 시 `AssetDownloader.<>d__19`의 worker/process 및 RetryController를 추가 분석.

## 패치 원칙
- 이미 효과가 확인된 패치는 유지.
- `d60` completion flag 재패치 금지.
- `AssetDownloader::IsCached()` 전역 true 패치 금지.
- `AssetDownloadingContext::Finish()` 자체 패치 금지.
- `RequestDownload 0x018473F0` 강제 cached 패치는 진단용으로만 취급.
- 다음 패치는 callback method와 Store 등 실제 실패 원인을 확인한 후 최소 범위로 결정한다.
